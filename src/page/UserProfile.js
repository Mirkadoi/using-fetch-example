import React from 'react';
// import firebase from 'firebase';

const UserProfile = () => {
    // const saveData = () => console.log(firebase.auth().currentUser);

    return (
        <div>
            Всем привет.
        </div>
    );
};

export default UserProfile;
